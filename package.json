{
  "name": "@chiwek/capacitor-active-progress",
  "publishConfig": { "access": "public" },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/chiwek/capacitor-active-progress.git"
  },
  "version": "1.0.6",
  "description": "Capacitor plugin for active progress notifications (Android progress bar, iOS Live Activity planned).",
  "main": "dist/esm/index.js",
  "types": "dist/esm/index.d.ts",
  "files": [
    "dist/",
    "android/",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf dist",
    "prepublishOnly": "npm run clean && npm run build"
  },
  "keywords": ["capacitor", "android", "notification", "progress", "live activity"],
  "author": "Chiwek",
  "license": "MIT",
  "capacitor": {
    "ios": {
      "src": "ios"
    },
    "android": {
      "src": "android"
    }
  },
  "peerDependencies": {
    "@capacitor/core": "^6.0.0 || ^7.0.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "rimraf": "^6.0.1"
  }
}
